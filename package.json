{
  "name": "minions-bundles-knowledge-workspace",
  "private": true,
  "version": "0.1.1",
  "description": "Knowledge management bundle \u2014 documents, concepts, citations",
  "type": "module",
  "license": "MIT",
  "author": "Mehdi Nabhani <mehdi@the-mehdi.com> (https://the-mehdi.com)",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "pnpm --filter @minions-bundles-knowledge/sdk run build && pnpm --filter @minions-bundles-knowledge/cli run build && pnpm --filter minions-bundles-knowledge-docs run build && pnpm --filter minions-bundles-knowledge-blog run build && pnpm --filter minions-bundles-knowledge-web run build",
    "deploy:blog": "pnpm --filter minions-bundles-knowledge-blog run build && netlify deploy --dir apps/blog/dist --prod",
    "deploy:docs": "pnpm --filter minions-bundles-knowledge-docs run build && netlify deploy --dir apps/docs/dist --prod",
    "deploy:web": "pnpm --filter minions-bundles-knowledge-web run build && netlify deploy --dir apps/web/dist --prod",
    "test": "pnpm --filter @minions-bundles-knowledge/sdk run test",
    "lint": "pnpm --filter @minions-bundles-knowledge/sdk run build && pnpm --filter @minions-bundles-knowledge/sdk run lint && pnpm --filter @minions-bundles-knowledge/cli run lint",
    "deploy": "pnpm run deploy:blog",
    "dev": "echo 'no dev'",
    "stop": "pkill -f 'node.*minions-bundles-knowledge' || true"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/mxn2020/minions"
  },
  "keywords": [
    "minions",
    "ecosystem",
    "compliance"
  ]
}