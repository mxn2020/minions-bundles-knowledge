[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-bundles-knowledge"
version = "0.1.1"
description = "Knowledge management bundle — documents, concepts, citations"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["knowledge","bundle","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-bundles-knowledge"
Repository = "https://github.com/mxn2020/minions-bundles-knowledge"

[tool.hatch.build.targets.wheel]
packages = ["minions_bundles_knowledge"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
